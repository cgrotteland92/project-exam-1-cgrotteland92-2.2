// authCheck.js
/*
const token = localStorage.getItem("authToken");

if (!token) {
  // Display message and redirect to login if not authenticated
  document.body.innerHTML =
    "<p>You must log in to view this page. Redirecting to login page...</p>";
  setTimeout(() => {
    window.location.href = "/login.html"; // Adjust path as needed
  }, 2000);
}
  */
